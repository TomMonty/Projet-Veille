/*

const { Events } = require("discord.js");

module.exports = {
    name: Events.MessageCreate,
    execute(message) {
        // ID de l'utilisateur cible (vous)
        const userId = "361897914710360065";

        // Vérifie si l'auteur est un bot
        if (message.author.bot) return;

        // Vérifie si le message est envoyé par vous (userId)
        if (message.author.id === userId) {
            message.reply("Ta gueule").catch(console.error); // Capture les erreurs éventuelles
        }
    },
};

*/
